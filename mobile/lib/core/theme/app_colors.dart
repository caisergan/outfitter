import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFFF3F5F7);
  static const backgroundElevated = Color(0xFFF9FAFB);
  static const surface = Color(0xFFFDFDFE);
  static const surfaceMuted = Color(0xFFE8EDF2);
  static const surfaceStrong = Color(0xFFD9E1E8);
  static const accent = Color(0xFF7E8E9D);
  static const accentStrong = Color(0xFF4A5C6C);
  static const text = Color(0xFF1F252B);
  static const textMuted = Color(0xFF6B7680);
  static const textSoft = Color(0xFF9099A2);
  static const line = Color(0xFFD7DEE5);
  static const lineStrong = Color(0xFFBFC9D2);
  static const success = Color(0xFF5E7164);
  static const warning = Color(0xFF87736A);
  static const danger = Color(0xFF9D6767);

  // Legacy aliases used across the app.
  static const mint = accent;
  static const lightMint = surfaceMuted;
  static const cream = background;
  static const blush = accentStrong;
}
